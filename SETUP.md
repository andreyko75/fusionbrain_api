# 🛠️ Инструкция по установке FusionBrain Image Generator

Подробное руководство по установке и настройке проекта для генерации изображений с помощью ИИ.

---

## 📋 Требования

### Системные требования:
* **Python 3.8+** (рекомендуется Python 3.11)
* **pip** (менеджер пакетов Python)
* **Git** (для клонирования репозитория)
* **Интернет-соединение** (для работы с API)

### Операционные системы:
* ✅ **macOS** 10.14+
* ✅ **Windows** 10+
* ✅ **Linux** (Ubuntu 18.04+, CentOS 7+)

---

## 🚀 Пошаговая установка

### Шаг 1: Подготовка системы

#### macOS:
```bash
# Установка Homebrew (если не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установка Python
brew install python@3.11

# Проверка версии
python3 --version
```

#### Windows:
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"
3. Проверьте установку:
```cmd
python --version
pip --version
```

#### Linux (Ubuntu/Debian):
```bash
# Обновление пакетов
sudo apt update

# Установка Python и pip
sudo apt install python3 python3-pip python3-venv

# Проверка версии
python3 --version
```

### Шаг 2: Клонирование проекта

```bash
# Клонирование репозитория
git clone https://github.com/ВАШ_ЛОГИН/fusionbrain_api.git

# Переход в директорию проекта
cd fusionbrain_api

# Проверка содержимого
ls -la
```

### Шаг 3: Создание виртуального окружения

```bash
# Создание виртуального окружения
python3 -m venv venv

# Активация виртуального окружения

# macOS/Linux:
source venv/bin/activate

# Windows:
venv\Scripts\activate

# Проверка активации (должен появиться (venv) в начале строки)
which python  # macOS/Linux
where python  # Windows
```

### Шаг 4: Установка зависимостей

```bash
# Обновление pip
pip install --upgrade pip

# Установка зависимостей из requirements.txt
pip install -r requirements.txt

# Проверка установленных пакетов
pip list
```

**Ожидаемый вывод:**
```
Package         Version
--------------- -------
certifi         2025.8.3
charset-normalizer 3.4.3
click            8.2.1
Flask            3.0.0
idna             3.10
itsdangerous     2.2.0
Jinja2           3.1.6
MarkupSafe       3.0.2
Pillow           10.0.1
python-dotenv    1.0.0
requests         2.31.0
urllib3          2.5.0
Werkzeug         3.1.3
```

### Шаг 5: Получение API ключей

1. **Перейдите на [FusionBrain.ai](https://fusionbrain.ai/)**
2. **Зарегистрируйтесь** или войдите в аккаунт
3. **Перейдите в раздел API**
4. **Создайте новый API ключ**
5. **Скопируйте API Key и Secret Key**

### Шаг 6: Настройка конфигурации

```bash
# Создание файла .env
touch .env  # macOS/Linux
# или
type nul > .env  # Windows

# Редактирование файла .env
nano .env  # macOS/Linux
# или
notepad .env  # Windows
```

**Содержимое файла .env:**
```env
# FusionBrain API Configuration
FUSIONBRAIN_API_KEY=ваш_api_ключ_здесь
FUSIONBRAIN_SECRET_KEY=ваш_secret_ключ_здесь
FUSIONBRAIN_URL=https://api-key.fusionbrain.ai/
```

**⚠️ Важно:** Замените `ваш_api_ключ_здесь` и `ваш_secret_ключ_здесь` на ваши реальные ключи!

### Шаг 7: Проверка установки

```bash
# Проверка структуры проекта
ls -la

# Ожидаемый результат:
# .env
# .gitignore
# README.md
# SETUP.md
# fusionbrain_client.py
# requirements.txt
# static/
# templates/
# venv/
# web_app.py
```

---

## 🧪 Тестирование установки

### Тест 1: Консольная версия

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Запуск консольной версии
python fusionbrain_client.py
```

**Ожидаемый результат:**
```
🎨 FusionBrain Image Generator
========================================
Введите описание изображения: 
```

### Тест 2: Веб-версия

```bash
# Запуск веб-версии
python web_app.py
```

**Ожидаемый результат:**
```
🚀 Запускаем FusionBrain Image Generator...
🌐 Веб-интерфейс будет доступен по адресу: http://localhost:8080
📱 Браузер откроется автоматически...
⏹️  Для остановки нажмите Ctrl+C
--------------------------------------------------
 * Serving Flask app 'web_app'
 * Debug mode: off
 * Running on http://127.0.0.1:8080
Press CTRL+C to quit
```

**Браузер должен открыться автоматически** с веб-интерфейсом.

---

## 🔧 Настройка для разработки

### Режим отладки (веб-версия)

Для включения режима отладки измените в `web_app.py`:

```python
# Строка 246
app.run(debug=True, host='127.0.0.1', port=8080, use_reloader=True)
```

### Изменение порта

Если порт 8080 занят, измените в `web_app.py`:

```python
# Строка 246
app.run(debug=False, host='127.0.0.1', port=9000, use_reloader=False)

# И в функции open_browser (строка 231)
webbrowser.open('http://localhost:9000')
```

### Настройка размера изображений

Для добавления новых размеров отредактируйте:

1. **В `web_app.py`** (строка 150-160) - добавьте новые размеры в `valid_sizes`
2. **В `templates/index.html`** - добавьте новые опции в `.size-options`
3. **В `static/css/style.css`** - при необходимости добавьте стили

---

## 🐛 Решение проблем

### Проблема: "python: command not found"

**Решение:**
```bash
# macOS/Linux
which python3
sudo ln -s /usr/bin/python3 /usr/bin/python

# Windows - переустановите Python с python.org
```

### Проблема: "pip: command not found"

**Решение:**
```bash
# macOS/Linux
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
python3 get-pip.py

# Windows - pip должен устанавливаться с Python
```

### Проблема: "Permission denied" при создании venv

**Решение:**
```bash
# Используйте --user флаг
python3 -m venv --user venv

# Или измените права доступа
chmod 755 venv/
```

### Проблема: "ModuleNotFoundError: No module named 'flask'"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: "Address already in use"

**Решение:**
```bash
# Найдите процесс, использующий порт
lsof -i :8080  # macOS/Linux
netstat -ano | findstr :8080  # Windows

# Остановите процесс
kill -9 PID  # macOS/Linux
taskkill /PID PID /F  # Windows

# Или измените порт в web_app.py
```

### Проблема: "API ключи не найдены в .env файле"

**Решение:**
1. Проверьте наличие файла `.env`
2. Убедитесь, что ключи указаны правильно
3. Проверьте, что нет лишних пробелов
4. Убедитесь, что файл находится в корне проекта

### Проблема: Браузер не открывается автоматически

**Решение:**
1. Откройте браузер вручную
2. Перейдите по адресу: http://localhost:8080
3. Проверьте настройки браузера по умолчанию

---

## 📱 Настройка для мобильных устройств

### Доступ с других устройств в сети

Измените в `web_app.py`:

```python
# Строка 246
app.run(debug=False, host='0.0.0.0', port=8080, use_reloader=False)
```

Теперь приложение будет доступно по IP-адресу вашего компьютера:
- http://192.168.1.100:8080 (замените на ваш IP)

---

## 🔒 Безопасность

### Рекомендации:

1. **Никогда не коммитьте файл `.env`** в Git
2. **Используйте сильные пароли** для API ключей
3. **Ограничьте доступ** к API ключам
4. **Регулярно обновляйте** зависимости
5. **Используйте HTTPS** в продакшене

### Проверка безопасности:

```bash
# Проверка, что .env не в Git
git status

# Проверка зависимостей на уязвимости
pip install safety
safety check
```

---

## 📊 Мониторинг и логи

### Включение подробных логов

Добавьте в `web_app.py`:

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### Мониторинг использования API

Добавьте счетчики запросов в код для отслеживания использования API.

---

## 🚀 Развертывание в продакшене

### Использование Gunicorn (Linux/macOS)

```bash
# Установка Gunicorn
pip install gunicorn

# Запуск
gunicorn -w 4 -b 0.0.0.0:8080 web_app:app
```

### Использование Waitress (Windows)

```bash
# Установка Waitress
pip install waitress

# Запуск
waitress-serve --host=0.0.0.0 --port=8080 web_app:app
```

---

## 📞 Поддержка

Если у вас возникли проблемы:

1. **Проверьте этот файл** - возможно, решение уже есть
2. **Проверьте README.md** - общая документация
3. **Создайте issue** в репозитории с подробным описанием
4. **Приложите логи ошибок** и информацию о системе

### Полезная информация для отчета об ошибке:

```bash
# Информация о системе
python --version
pip --version
uname -a  # macOS/Linux
systeminfo  # Windows

# Список установленных пакетов
pip list

# Логи ошибок
python web_app.py 2>&1 | tee error.log
```

---

*Удачной установки и использования! 🎨*
