# 🎨 FusionBrain Image Generator (Python + FusionBrain API)

Небольшой учебный проект: консольный и веб-клиент на Python для автоматической генерации изображений с помощью ИИ через FusionBrain API (модель Kandinsky).

Проект создан в рамках изучения **API для генерации изображений с помощью ИИ**.

---

## 🚀 Возможности

### Консольная версия (`fusionbrain_client.py`)
* Генерация изображений по текстовому описанию
* Автоматическое сохранение в папку `output/`
* Размер изображения: 1024x1024 пикселей (1:1)
* Поддержка русского и английского языков
* Красивый интерфейс с эмодзи и прогресс-индикаторами

### Веб-версия (`web_app.py`)
* **Современный веб-интерфейс** с адаптивным дизайном
* **Выбор размеров изображения** (6 вариантов):
  - 512×512 (квадрат малый)
  - 768×768 (квадрат средний) 
  - 1024×1024 (квадрат большой)
  - 512×768 (портрет)
  - 768×512 (пейзаж)
  - 1024×768 (широкий)
* **Прогресс-бар** с анимацией
* **Кнопка копирования** изображения в буфер обмена
* **Кнопка скачивания** изображения
* **Toast-уведомления** об успехе/ошибках
* **Автоматическое открытие браузера** при запуске

---

## 📂 Структура проекта

```
fusionbrain_api/
├── fusionbrain_client.py    # консольный клиент
├── web_app.py              # веб-приложение Flask
├── .env                    # ключи API (не попадает в Git)
├── .gitignore             # исключает медиафайлы и .env
├── requirements.txt        # зависимости
├── templates/             # HTML шаблоны
│   └── index.html         # главная страница
├── static/                # статические файлы
│   ├── css/
│   │   └── style.css      # стили
│   └── js/
│       └── app.js         # JavaScript логика
├── output/                # готовые изображения (не в Git)
├── venv/                  # виртуальное окружение (не в Git)
├── README.md              # документация
└── SETUP.md               # инструкция по установке
```

---

## ⚙️ Установка и запуск

### 1. Клонируйте проект:
```bash
git clone https://github.com/ВАШ_ЛОГИН/fusionbrain_api.git
cd fusionbrain_api
```

### 2. Создайте и активируйте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate   # Mac/Linux
venv\Scripts\activate      # Windows
```

### 3. Установите зависимости:
```bash
pip install -r requirements.txt
```

### 4. Получите API-ключи на FusionBrain и добавьте их в `.env`:
```env
FUSIONBRAIN_API_KEY=ваш_api_ключ
FUSIONBRAIN_SECRET_KEY=ваш_secret_ключ
FUSIONBRAIN_URL=https://api-key.fusionbrain.ai/
```

### 5. Запустите приложение:

#### Консольная версия:
```bash
python fusionbrain_client.py
```

#### Веб-версия:
```bash
python web_app.py
```
*Браузер откроется автоматически по адресу: http://localhost:8080*

---

## 📸 Пример работы

### Консольная версия

**Запуск:**
```bash
python fusionbrain_client.py
```

**Вывод:**
```
🎨 FusionBrain Image Generator
========================================
Введите описание изображения: медведь залез на дерево в лесу

🚀 Генерируем изображение: 'медведь залез на дерево в лесу'
⏳ Это может занять несколько минут...
📋 ID задачи: 4cbd2df5-3999-45ad-a2a6-7f84e39f1d90
Статус: INITIAL (попытка 1/30)
Статус: INITIAL (попытка 2/30)
Статус: DONE (попытка 3/30)
✅ Изображение успешно сохранено: output/generated_20250906_164407.png
📁 Размер: 1024x1024 пикселей
```

### Веб-версия

**Запуск:**
```bash
python web_app.py
```

**Вывод:**
```
🚀 Запускаем FusionBrain Image Generator...
🌐 Веб-интерфейс будет доступен по адресу: http://localhost:8080
📱 Браузер откроется автоматически...
⏹️  Для остановки нажмите Ctrl+C
--------------------------------------------------
 * Serving Flask app 'web_app'
 * Debug mode: off
 * Running on http://127.0.0.1:8080
Press CTRL+C to quit
```

**В браузере:**
- Современный интерфейс с выбором размеров
- Поле для ввода промпта с счетчиком символов
- Прогресс-бар с анимацией
- Отображение готового изображения
- Кнопки копирования и скачивания

---

## 🛡️ Важно

* `.env` и другие секретные файлы не должны попадать в публичный репозиторий
* API-ключи у каждого свои — проект без них работать не будет
* Интернет соединение необходимо для работы с API
* Генерация изображений может занять 1-3 минуты
* Максимальная длина промпта: 1000 символов

---

## 📖 Используемые технологии

### Backend:
* **Python 3.8+**
* **Flask** (веб-фреймворк)
* **requests** (HTTP запросы)
* **python-dotenv** (переменные окружения)
* **Pillow** (обработка изображений)

### Frontend:
* **HTML5** (разметка)
* **CSS3** (стили с градиентами и анимациями)
* **JavaScript ES6+** (интерактивность)
* **Font Awesome** (иконки)

### API:
* **FusionBrain API** (генерация изображений)
* **Kandinsky 3.0** (модель ИИ)

---

## 🎨 Поддерживаемые форматы

**Входные данные:**
- Текстовые описания на любом языке (до 1000 символов)
- Эмодзи в промптах

**Выходные данные:**
- **PNG** изображения
- Размеры: 512×512, 768×768, 1024×1024, 512×768, 768×512, 1024×768
- Качество: высокое (до 1024 пикселей на сторону)

---

## 🐛 Устранение неполадок

**Ошибка "API ключи не найдены в .env файле":**
* Проверьте наличие файла `.env`
* Убедитесь, что API ключи указаны правильно
* Получите ключи на [FusionBrain.ai](https://fusionbrain.ai/)

**Ошибка "Address already in use":**
* Порт 8080 занят другим приложением
* Измените порт в `web_app.py` (строка 246)
* Или остановите другое приложение на этом порту

**Ошибка "Превышено максимальное количество попыток":**
* API может быть перегружен
* Попробуйте позже или проверьте интернет-соединение
* Убедитесь, что API ключи действительны

**Браузер не открывается автоматически:**
* Откройте браузер вручную
* Перейдите по адресу: http://localhost:8080
* Проверьте, что веб-сервер запущен

**Ошибка "Flask не найден":**
* Активируйте виртуальное окружение
* Установите зависимости: `pip install -r requirements.txt`

---

## 🔧 API Endpoints (веб-версия)

* `GET /` - главная страница
* `POST /generate` - запуск генерации изображения
* `GET /status/<request_id>` - проверка статуса генерации
* `GET /image/<filename>` - отображение изображения
* `GET /download/<filename>` - скачивание изображения

---

## 📊 Прогресс выполнения

### Консольная версия:
* 🔍 **Получение пайплайна** (1/3)
* 🚀 **Запуск генерации** (2/3)
* ⏳ **Ожидание результата** (3/3)

### Веб-версия:
* 🔍 **Поиск модели** (1/6)
* 🚀 **Запуск генерации** (2/6)
* ⏳ **Ожидание обработки** (3/6)
* 📊 **Проверка статуса** (4/6)
* 💾 **Сохранение файла** (5/6)
* ✅ **Отображение результата** (6/6)

---

## 🌟 Особенности

* **Два режима работы**: консольный и веб-интерфейс
* **Автоматическое открытие браузера** при запуске веб-версии
* **Красивый дизайн** с градиентами и анимациями
* **Адаптивная верстка** для всех устройств
* **Обработка ошибок** с понятными сообщениями
* **Прогресс-индикаторы** для отслеживания процесса
* **Копирование в буфер обмена** и скачивание изображений

---

## 📝 Лицензия

Этот проект создан в образовательных целях. Используйте ответственно и в соответствии с условиями использования FusionBrain API.

---

## 🤝 Вклад в проект

Приветствуются предложения по улучшению:
* Добавление новых размеров изображений
* Поддержка дополнительных стилей
* Улучшение пользовательского интерфейса
* Оптимизация производительности

---

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Устранение неполадок"
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность API ключей
4. Создайте issue в репозитории

---

*Создано с ❤️ для изучения API генерации изображений*